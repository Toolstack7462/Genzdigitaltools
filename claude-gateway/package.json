{
  "name": "genz-claude-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Isolated Gen Z reverse-proxy gateway for Claude (claude.ai) — same generic engine as proxy-gateway, deployed standalone on claude1.genzdigitalstore.com with its own .env and cookie vault.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
