import { useState } from 'react';
import { useBusinessCrm } from '../BusinessCrmContext';
import { useResource } from '../hooks';
import { formatDate, formatMoney, monthRange } from '../constants';
import { Card, ErrorState, Field, Input, Loading, PageHeader, Status, Table } from '../components/ui';
export default function Cashbook(){const crm=useBusinessCrm();const initial=monthRange();const[from,setFrom]=useState(initial.from);const[to,setTo]=useState(initial.to);const r=useResource(()=>`/reports/cashbook?currency=${crm.currency}&from=${from}&to=${to}`,[crm.currency,from,to]);if(r.loading)return <Loading/>;if(r.error)return <ErrorState message={r.error} onRetry={r.reload}/>;return <><PageHeader title="Cashbook" description="Actual client receipts, vendor payments, reversals and operating expenses in one cash movement ledger."/><div className="bcrm-filterbar"><Field label="From"><Input type="date" value={from} onChange={e=>setFrom(e.target.value)}/></Field><Field label="To"><Input type="date" value={to} onChange={e=>setTo(e.target.value)}/></Field></div><Card className="flush"><Table rows={r.data?.rows||[]} columns={[{key:'entry_date',label:'Date',render:x=>formatDate(x.entry_date)},{key:'entry_type',label:'Type',render:x=><Status tone={Number(x.signed_amount)>=0?'success':'warning'}>{String(x.entry_type).replaceAll('_',' ')}</Status>},{key:'notes',label:'Details'},{key:'method',label:'Method'},{key:'reference',label:'Reference'},{key:'signed_amount',label:'Movement',render:x=><strong>{formatMoney(x.signed_amount,crm.currency)}</strong>},{key:'running_balance',label:'Running balance',render:x=>formatMoney(x.running_balance,crm.currency)}]} /></Card></>}
